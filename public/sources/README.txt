Five screenshots. Any of .jpg / .jpeg / .png / .webp works — the card tries
each in turn, so the extension does not have to match exactly.

  f3          github.com/center-for-threat-informed-defense/fight-fraud-framework
  owasp       genai.owasp.org/resource/owasp-top-10-for-agentic-applications-for-2026/
  ap2         ap2-protocol.org/ap2/security_and_privacy_considerations/
  rbi         rbi.org.in/Scripts/PublicationsView.aspx?id=23810
  echoleak    nvd.nist.gov/vuln/detail/CVE-2025-32711

Capture the top of each page, roughly 16:10, around 1200px wide. Do not include
your own browser chrome — the card draws its own macOS window frame.

The five research papers need no screenshots. They render as a single window
with one clickable row each, because five arXiv abstract pages look identical
and tell a judge nothing.

A missing file falls back to a document sketch, so cards and links keep working
while the set fills up.
